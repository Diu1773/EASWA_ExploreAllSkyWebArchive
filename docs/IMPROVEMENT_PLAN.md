# EASWA 개선 계획 (Improvement Plan)

작성 2026-07-20. 현장·동료 피드백 + QA 스윕 발견을 하나로 모은 백로그.
연수(2026-07-22)를 기준으로 **연수 전 / 연수 후 / 공공 배포 전** 3단계로 나눈다.

## 진행 현황 (2026-07-20 야간 자율 — /goal)

**Phase 0·1·2 전부 구현·검증 완료.** 커밋 4c289ff~5b6f7a9.

| 항목 | 상태 |
|------|------|
| P0-1 depth→Rp/R* 유도 | ✅ Step 5 accent 박스 (√0.02105 = 0.1451) |
| P0-2 공전주기 아카이브 표기 | ✅ Step 5·6 'NASA 아카이브 값 / 측정값 아님'. 헬프텍스트도 '주기 비교' 제거 |
| P1-1 파이프라인 개념도 | ✅ Step 0 7단계 + role 색(자동/조절/판단) + SPOC 배지 |
| P1-2 용어집 | ✅ Step 0 접이식 (cutout·sector·aperture·FWHM·ROI) |
| P2-1 사이트 피드백 | ✅ Step 6 접이식 별점+서술. 시트 별도 컬럼. 유닛 13 passed |
| P2-2 설문 동기 문항 | ✅ 3-1 추가 → 파일럿 반영해 필수 해제 |
| P2-3 자동저장 동시성 | ✅ 이미 구현·검증돼 있었음(retry+jitter, 20명 19.0s 실측) |

**페르소나 검증**: survey-pilot-test(3-1 필수 결함 발견·반영), learner-ux-review(개념도 색 힌트 반영).

**미반영 권고 (구조 변경 — 사용자 승인 후)**:
- [주요] Step 6 사이트 피드백이 탐구 기록(본론) '위'에 위치. 탐구 완료 후 도구 평가가
  자연스러우므로 ReflectionPanel/record fields '아래'로 이동 권고. renderStepBody 구조
  변경이라 승인 후.
- [경미] Step 0 정보량 증가(3D+광도곡선+개념도+용어집+할일+수업적용) — 중고생 스크롤 피로.
  개념도 가치가 커 유지하나, 순서·접이 재고 여지.
- [경미] Step 5 유도 박스 '√0.02105'에서 2.105%→0.02105 환산이 초보에게 살짝 점프.

**⚠️ 사용자 조치** (2026-07-24 갱신 — 상세는 docs/TRAINING_DAY_CHECKLIST.md):
- ~~교사 설문 재배포~~ → **완료**: 신규 폼 생성됨(3-1 포함, 응답 URL `…1FAIpQLSf9gHF…`). QR도 신규 폼으로 재생성(0db610a).
- 기록 sink 재배포(편집→새 버전)는 **아직**: 앱이 쓰는 `AKfycbyAV…` 배포에 site 컬럼 코드 반영 필요.
  (전이라도 앱 정상 — site 값만 조용히 버려짐. "새 배포" 금지: URL 바뀜, 이미 2회 발생)
- 기록 시트 헤더: U1 `site_rating`, V1 `site_feedback` 수동 입력.

**후속 리뷰 (2026-07-24)**:
- 전체 설문 방법론 재검토(대학원생 페르소나): 3-1 추가가 기존 정제(역문항 12·15, 유도문항 제거)를
  흔들지 않음 확인. 유일 잔여 = 3-1 선택지 a·b 변별(경미) — b 문구 교체안 제시, **사용자 결정 대기**.
- QA 4차 스윕(25장, 1440/1900): 신규 발견 0건 — **수렴**.

---


> 판단 기준은 항상 CLAUDE.md의 5개 설계원리. 각 항목에 원리 번호와 **정직성 노트**(있으면)를 단다.
> 정직성 노트가 달린 항목은 구현 전에 반드시 방침을 합의한다 — 계산식·전처리를 지어내면 철칙(합성·근사 금지)에 걸린다.

---

## Phase 0 — 연수 전 (D-2, 저비용·고효과만)

큰 개발은 이틀에 무리. 정직성·저비용·즉시효과만 손댄다.

| ID | 항목 | 원리 | 비용 | 상태 |
|----|------|------|------|------|
| P0-1 | **Step 5: 식 깊이 → Rp/R\* 유도 과정 표시** | 3,4 | 낮 | 대기 |
| P0-2 | **Step 5: 공전주기 "계산 안 함" 정직 표기** | 3 | 낮 | 대기 |

- **P0-1**: `depth = (Rp/R*)²` → `Rp/R* = √depth`. 예: `depth 2.105% → √0.02105 = 0.1451`.
  Step 0에 이미 있는 `깊이 ∝ (Rp/R★)²` 공식이 Step 5에서 실제 숫자로 닫힌다.
  fit이 실제로 산출하는 값이라 계산과정 제시가 **정확**하다.
- **P0-2 (정직성)**: 공전주기는 앱이 **계산하지 않는다** — NASA 카탈로그값을 위상접기·fit 입력으로 쓴다.
  근거 3중 확인: ① `period_days`가 `target.get("period_days")`에서 옴 ② fit 자유파라미터는
  Rp/R*·a/R*·경사각뿐, 주기는 고정 ③ Step 5 비교표에서 내측정=문헌 +0.0% 완전일치.
  물리적으로도 단일 TESS sector엔 식이 1회뿐 → 주기는 식 사이 간격이 있어야 나온다.
  → "주기는 알려진 값 사용, 단일 관측으론 못 구하는 이유"를 명시. 계산식 금지.

---

## Phase 1 — 연수 직후 (개념 보강, 중비용)

현장에서 가장 크게 요청된 "왜"의 가시화.

| ID | 항목 | 원리 | 비용 | 근거 |
|----|------|------|------|------|
| P1-1 | **Step 0 파이프라인 개념도** | 1,3,5 | 중 | 현장 피드백 |
| P1-2 | **용어 첫 등장 설명 (aperture·ROI·FWHM)** | 3 | 중 | 현장 피드백 |

### P1-1 파이프라인 개념도 — 골격 (합의 필요)

각 단계의 "왜"와 "누가"를 담은 개념도. 단순 순서도 아님. Step 0을 2단으로
(현상 애니 + 파이프라인 개념도).

| 단계 | 왜 하는가 | 주체 배지 |
|------|-----------|-----------|
| 관측 (TESS 위성) | 별빛을 시계열로 기록 | 위성 |
| 계기 보정 (bias·dark·flat·우주선) | 검출기 특성·잡광 제거 | **SPOC 이미 완료** |
| 자료 취득 (MAST→cutout) | 필요한 별 주변만 잘라옴 | 플랫폼 자동 |
| 구경측광 (구경합−배경) | 별빛이 PSF로 퍼짐→다 모아야 진짜 밝기 | 플랫폼 |
| 차등측광 (목표÷비교) | 공통 흔들림 상쇄 | 플랫폼 |
| 정규화·모델적합 (baseline+batman) | 식 깊이·Rp/R* 추출 | 플랫폼 |
| 기준값 비교 (NASA) | 내 측정 검증 | 학습자 |

- **정직성 노트**: TESS cutout은 SPOC가 계기 보정을 **이미 끝낸** 자료다. 학습자·플랫폼이 하는
  "전처리"는 배경 제거(구경측광)·systematics 제거(차등측광)·정규화뿐. 개념도에
  "계기 보정 완료 → 우리는 왜 필요한지만 이해"로 표시해야 정확.
- **원리 2 ↔ 3 균형**: 취득·전처리는 플랫폼이 흡수(원리 2)하는 게 의도. 개념도의 목적은
  "직접 하게"가 아니라 "자동 처리하되 존재·이유를 보이게"(원리 3). 자동 단계는 흐리게,
  학습자 판단 단계(비교성 선택·해석)는 강조.

### P1-2 용어 설명
- `aperture`·`ROI`가 **Step 0 "이 탐구에서 할 일"에서 설명 없이 영어로 첫 등장**. `FWHM`는 Step 3.
- 첫 등장 지점에 hover 정의 또는 괄호 풀이. 3~5개만 절제(툴팁 남발은 과부하 = 원리 3 역효과).

---

## Phase 2 — 공공 배포 전

| ID | 항목 | 원리 | 비용 | 근거 |
|----|------|------|------|------|
| P2-1 | **Step 6 사이트 피드백 수집** | 5 | 중 | 사용자 (공공 배포 대비) |
| P2-2 | 설문: "왜 실제 관측자료를 쓰려는가" 문항 | — | 낮 | 동료 |
| P2-3 | 자동저장 동시성 (여러 명 동시 사용) | — | 중 | 이전 세션 보류 |

- **P2-1 (사이트 피드백)**: 학습 기록과 **분리**. 시트 별도 컬럼 `site_feedback`. 선택·접이식
  ("이 도구가 어땠나요 (선택)"). 탐구 끝낸 사람만 도달 → 질 높음. 별점 + 자유서술 1칸 정도.
  연수엔 불필요(대면 구두 수집).
- **P2-2 (설문)**: 배경조사 문항3(경험 유무)과 **다른 축**(동기/요구). 유무 무관하게
  "실제 관측자료가 수업에 필요하다고 보는 이유"로 물으면 표본↑·논거↑. 단 설문은 이미
  파일럿 정제 완료 → 개방형 추가는 응답부담. 필수/선택·위치 재검토(survey-pilot-test).

---

## Phase 3 — 2회차 시연 종료 후 (2026-09-06 심사에서 나온 것)

`/ars-reviewer` 5인 심사와 소유자 검토에서 확정된 항목. 대응 대조표는
`docs/심사대응_v15_2026-09-06.md`, 웹 작업 지시는 `docs/웹수정_프롬프트_2026-09-07.md`.

**⛔ 2026-09-07 2회차 시연이 끝날 때까지 `main` 푸시 금지** — Render 자동 배포로 시연 중 화면이 바뀐다.

### P3-1 문헌 반지름비를 아카이브에서 직접 받기 (웹, 우선)

지금은 백엔드가 `pl_trandep`(식 깊이)만 받아 오고 프런트가 √해서 반지름비를 만든다. 그런데
아카이브 대표값 테이블(`pscomppars`)은 깊이와 반지름비를 서로 다른 출처에서 조합한다.

| 대상 | √(깊이) | 같은 행의 `pl_ratror` | 차이 |
|---|---|---|---|
| **WASP-6 b** | 0.1552 (깊이 2.408%, ExoFOP) | **0.1446 ± 0.0009** (Kokori et al. 2023) | 7.3% |
| WASP-121 b | 0.1226 | 0.122551 | 거의 같음 |
| WASP-151 b | 0.1033 | 0.1011 | 2% |

WASP-6 b는 시연 기본 예제다. 2026-07-24 조사에서 학습자 14명 중앙값이 0.1457이었는데 화면이
0.1552를 문헌값으로 보여 **6% 얕게 쟀다고 읽혔다.** 아카이브 반지름비와 비교하면 +0.8%로 사실상
일치하므로, 측광·적합이 아니라 비교 기준이 어긋난 것이다.

- 질의에 `pl_ratror, pl_ratrorerr1, pl_refname` 추가 → 있으면 그것을 기준값으로, 없을 때만 √(깊이)
- **출처를 값 옆에 항상 표시**한다. 아카이브 대표값은 여러 관측을 종합한 것일 수 있어(WASP-6 b는
  지상망·문헌·Kepler/K2/TESS 약 18,000점 종합), 학습자가 쓰는 TESS 컷아웃 하나와 같은 조건이 아니다.
  값을 고르는 게 아니라 조건 차이를 드러내는 것이 목적이다.
- 조사 자료(7/24, 9/6~7)는 옛 동작으로 수집된 것이므로 재해석하지 않는다. 원고 5.5에 이 한계를 적었다.

### P3-2 KMTNet 자료 카드의 `Placeholder adapter` (웹)

`configs.ts` 918행. 실제로는 관측소별 `.pysis` 측광 테이블과 실제 관측 프레임을 쓴다.
「KMTNet 공개 아카이브 (CTIO·SAAO·SSO)」로 교체.

### P3-3 「흔들림」 표현 (웹, 선택)

산포를 가리키는 말로 6곳 남아 있다. **문항 지문·선택지(`configs.ts` 78·160·166)는 건드리지 말 것** —
`score_app_records.py`의 정답 키와 묶여 있어 조사 채점이 깨진다. 나머지만 판단.

### P3-4 이해상충 진술 (원고)

반론자 심사 지적. 플랫폼 개발·조사 설계·진행·응답 분석을 모두 연구자 1인이 수행했는데, 원고는
이를 부호화 신뢰도 문제로만 다룬다. 참여자가 개발자 앞에서 응답하고 진행자 안내를 받은 조건이
반응을 우호적인 쪽으로 이끌 수 있다는 점, 대면 세션의 완주율도 같은 영향을 받는다는 점을 5.5에
두 문장으로 넣는다. **조판(`EASWA_논문_v15_조판.html`)을 다시 뽑아야 하므로 2회차 수치 갱신과
함께 처리하는 편이 낫다.**

### P3-6 회차별 배포본 (원고 4.7·3.6 확인)

`docs/DEMO_VERSION_RECORD.md`에 1회차 `f02b77d`(index-Cr-IO1Q1.js), 2회차 `7ff424d`(index-CVSBXqxm.js)로
**서로 다른 빌드**가 기록되어 있다. 현재 원고 4.7은 「두 회차는 같은 배포본을 사용하였다」로 쓰여 있으므로,
두 빌드 사이에 무엇이 바뀌었는지 확인해 문장을 사실에 맞추거나 차이를 밝혀야 한다. 2회차 수치를
반영할 때 함께 처리한다.

### P3-5 투고 규정 확인 (소유자)

한 번 보면 네 가지가 함께 풀린다 — IRB 항목 유무(ERP 추진계획에는 없음을 확인), DOI 필수 여부
(현재 영문 46건 중 3건), 참고문헌 목록의 `et al.` 허용(현재 8건), 국문 저자 표기 규칙
(함경림만 5인 전원 나열, 나머지는 「외」). 상세는 `docs/인용검사_v15_2026-09-06.md`.

---

## 보류 (시연 제외 / 판단 완료)

| 항목 | 사유 |
|------|------|
| KMTNet 대표 질문 템플릿값 | 시연 안 함 (2026-07-20 사용자 결정) |
| KMTNet YouTube 폴백 | 시연 안 함 |
| Step 3 구경 수치 두 개(r=12 시뮬 vs 2.5 실제) 병존 | 주석이 일부 해소. 혼동 소지 남음 — Phase 1 개념도로 흡수 가능 |

---

## 이번 세션(2026-07-18~20)에 이미 완료된 것 (참고)

- 비교성 QC 대상제외 진단(비교성÷나머지) + 문구 AI티 제거
- 차등측광 개념 설명 + 옛 문항 교체 + '앙상블'/대기 오류 정리
- Step 1 생각해보기를 작업 뒤로 (불러오기 fold 위로)
- Step 5 재구성: 문헌값 차이 4열 + 진단값 접기 + 플롯 전폭 + 영문 플래그 한국어화
- Step 6 행성명 대소문자(WASP-6 B→b), '내가 분석한 결과'→'분석 결과', CTA 라벨
- 익명 고지 대비 3.49→6.37, logged_in 열(전 체인 검증), 싱크 URL 갱신
- fit 있는데 Step 5·6 새로고침 튕김 버그 수정 + QA 스윕 로봇 수리
